__pycache__/
*.pyc
*.lmz
log.txt
results/

# `lmsluice/lmz_merge.ptx` is deliberately NOT ignored: it is prebuilt for
# sm_52 and shipped in the wheel, so a machine with a driver and no CUDA
# toolkit still gets the device decode path. See pyproject's package-data.
dist/
build/
*.egg-info/
