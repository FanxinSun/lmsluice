# SLUICE-B1 specialized deployment evaluation

B1 is independent of the affordability evaluation. It compares deployment
workflows and records technical, integration and evidence limits. The current
correction campaign is identified by the exact `source_commit` in
`manifest.json` and the export-substituted marker in
[`final-evidence.json`](final-evidence.json); its complete rows and records
are retained in the probe archive and in the current final cache fast-path
review report:
`/home/rog/.codex/handover/2026-09-13-lmsluice-cache-fastpath-final-review.REPORT.md`.
reference clean tracked-export campaign
(`/tmp/lmsluice-device-readiness.WKXRxT`) used the frozen workload and checked
consumer output hashes against
the plain artifact hash. The rows and per-request readiness records are in the
run directory retained by the probe.

| candidate | technical evidence | integration/support limit | customer evidence | recommendation |
|---|---|---|---|---|
| Restricted/private distribution | Local sealed archive loaded successfully; 0.002193 s total and exact output hash | Key distribution and policy remain external | Unavailable | Retain the mechanism evidence and defer a customer claim |
| Bandwidth-constrained population | Local HTTP range path: 0.014529 s; no-range fallback: 0.008157 s; both exact output hashes | Reconnect policy, WAN behavior and durable publication remain unmeasured | Unavailable; loopback is only a proxy | Pursue a named target validation before making a market claim |
| Switching specialized models | Language subset 0.001820 s; specialist subset 0.001936 s with the synthetic periodic task armed; output hashes passed | Residency, scheduling and runtime integration remain external | Unavailable; synthetic tasks are not application acceptance | Defer until a named runtime and switching cost are measured |

The correction also exercises the cache workflow: initial miss/fetch, warm
reuse, changed-artifact stale refusal and forced refresh, interruption with
restart-from-zero semantics, and range/no-range fallback. It cross-links the
wrong-key and tampered-ciphertext evidence. Durable resume remains explicitly
unimplemented.

The optional lmz arm is labeled separately from the stdlib-coded route and is
run only from an already configured interpreter. The route-prediction rows use
disjoint seed-17 calibration and seed-29 held-out evaluation artifacts; their
local sign errors and wrong-route cost remain mechanism evidence.

The private workflow exercised the existing authenticated local mechanism. No
historical credential, external secret, cloud write or real customer system was
used. The authenticated store path remains represented by the existing local
regression fixtures; real hosted-store evidence is unavailable in this run.

The switching rows now include positive periodic ticks before and during each
load and a comparable resident/no-load control. They remain scheduler
interference evidence rather than a voice deadline or application SLA.

The range and no-range cases establish that the source abstraction can restore
the coded fixture through both response shapes on loopback. Their timings are
not WAN or device measurements. The no-range result being faster here reflects
the tiny local fixture and server overhead; it says nothing about a constrained
link. Likewise, the periodic task reports scheduler interference only; this
sentence describes scheduler interference only. The initial reference sample
recorded zero periodic ticks; the correction campaign requires positive ticks
before and during each load, with a resident control. This is not a voice
deadline, speech quality result or portable-device SLA.

The candidate comparison remains `PASS` as an evidence-collection exercise,
while the strategic evidence remains `INCONCLUSIVE`: no candidate has a named
buyer, consumer acceptance signal, deployment cost, support burden from a real
fleet, or target-device measurement. The manager retains the niche decision.
The next B1 brief should name one target workflow, its owner, an approved
artifact source, the consumer acceptance check and the customer evidence that
would make the result decision-grade.
